<?php

  // Exemplos de palavras reservadas

  // echo, insteadof, else, interface, namespace, pow, _DIR_, _FILE_, endif
  // print, private, protected, and, require,public, as, break, case, for
  // finally, switch, throw e etc;

  /*function echo() {
    return true;  // echo é palavra reservada/
  }

  function while() {
    return true; // while é palavra reservada
  }*/

  function whilenew() {
    return true;
  }